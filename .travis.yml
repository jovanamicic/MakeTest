language: java
install: true
script: "mvn clean verify"
jdk:
  - oraclejdk8
services:
  - mysql
before_script:
- mysql -u root -e "CREATE DATABASE root;"
- mysql -u root -e "CREATE USER 'root'@'localhost' IDENTIFIED BY 'root';"
- mysql -u root -e "GRANT ALL ON root.* TO 'root'@'localhost';"